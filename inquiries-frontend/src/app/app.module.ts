// This file is no longer needed in Angular 20 standalone setup
// But keeping it for compatibility

import { NgModule } from '@angular/core';

@NgModule({
  // Empty module for compatibility
})
export class AppModule { }